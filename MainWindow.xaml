<Window x:Class="VuMeter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:VuMeter"
        Title="VU Meter" Height="500" Width="1080"
        ResizeMode="CanResize"
        MinHeight="360" MinWidth="780"
        Background="#0d0a06"
        WindowStyle="SingleBorderWindow"
        Topmost="False">

    <Window.Resources>
        <SolidColorBrush x:Key="LabelBrush" Color="#a89060"/>
    </Window.Resources>

    <Grid Margin="20,18,20,14">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <local:AnalogVuMeter x:Name="LeftMeter"  Grid.Column="0" Label="L"/>
            <local:AnalogVuMeter x:Name="RightMeter" Grid.Column="2" Label="R"/>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0">
            <Ellipse x:Name="StatusDot" Width="9" Height="9" Fill="#333333" Margin="0,0,8,0" VerticalAlignment="Center"/>
            <TextBlock x:Name="StatusText" Text="No audio device" Foreground="{StaticResource LabelBrush}"
                       FontFamily="Consolas" FontSize="11" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
